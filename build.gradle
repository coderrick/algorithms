/*
 * This build file was auto generated by running the Gradle 'init' task
 * by 'ProgrammingPC' at '8/3/16 11:49 PM' with Gradle 2.9
 */
apply plugin: 'java'
apply plugin: 'application'
// In this section you declare where to find the dependencies of your project
repositories {
    // Use 'jcenter' for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
    jcenter()
}

//A part of Java plugin
sourceSets.main.java.srcDir "src/main"
/*
  sourceSets{
      main{
          java{
              srcDir ("src/main")
          }
      }
  }
*/

// In this section you declare the dependencies for your production and test code
dependencies {
    compile 'org.slf4j:slf4j-api:1.7.13'
    testCompile 'junit:junit:4.12'
}

/*
  closure{
      A closure in Groovy is an open, anonymous, block of code that can take arguments, 
      return a value and be assigned to a variable. A closure may reference variables 
      declared in its surrounding scope.

      Ex.
      def say = { what ->
          println what
        }
       say "Hello World"
     }

     Or use `it` to not specify variable
     def say = { println it }
     say "Hello World"
*/